sudo tee /home/nethmi/DevOps/thingsboard/thingsboard.yml > /dev/null <<'EOL'
spring:
  datasource:
    url: "jdbc:postgresql://host.containers.internal:5432/thingsboard"
    username: "nethmi"
    password: "123456"
  jpa:
    hibernate:
      ddl-auto: "validate"
EOL
